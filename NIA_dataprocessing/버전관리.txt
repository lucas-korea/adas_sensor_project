ver0

Frame_TimeStamp를 서로 다른 폴더에 있는걸 가정하고 코딩
진행상황 표시 x
pickle 코드 남아있는 등 여러 legacy 코드정리 미흡

ver1 없음

ver2
Frame_TimeStamp를 서로 한 폴더에 있는걸 가정하고 코딩
진행상황 표시 o
legacy 코드정리 1차 완료
matcing된 데이터 정리한 폴더 선택 시점을 빨리 하여, 보다 효율적인 실행프로세스 가능.

ver3

high res, low res maching 과정 추가
4ch 이미지를 다 같이 끌어오는 과정 추가

ver4

아래와 같이 취득 되었을 때, 1149463_L이 1149365_H랑 매칭되어버린다. 1149463은 어쨋거나 range=50 안에 해당하는 1149465_H 라이다가 존재하기 때문에.
1149365_H
1149465_H            1149463_L
---                  ----

ver4.1

matching_high_low()
           if len(LowLidar_stamp_List[mask_all]):
                shutil.copy2(lidar_ts_path + '\\' + LowLidarList[mask_all][0], # 순서대로 가져오는것이 아니라, range 안에서 매칭되는 파일을 복사
                             pcd_move_dir + '\\' + '_'.join(LowLidarList[mask_all][0].split('_')[:2])[2:]

research version
ver2에서 파생
연구용으로 작성. PCD가 제대로 추출 안되었기 때문에, 따로 다시 추출된 pcd를 matching된 png에 맞게 matched pcd를 뽑아주는 코드.